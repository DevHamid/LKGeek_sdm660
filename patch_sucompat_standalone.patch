diff --git a/kernel/feature/sucompat.c b/kernel/feature/sucompat.c
index aa4a13eb..c015d531 100644
--- a/kernel/feature/sucompat.c
+++ b/kernel/feature/sucompat.c
@@ -138,14 +138,8 @@ static char __user *empty_user_path(void)
     return userspace_stack_buffer("", sizeof(""));
 }
 
-static bool is_ksud_exists()
+static bool is_ksud_exists(void)
 {
-    struct path path;
-
-    if (kern_path(KSUD_PATH, 0, &path) < 0) {
-        return false;
-    }
-    path_put(&path);
     return true;
 }
 
@@ -381,7 +375,7 @@ static inline int do_ksu_handle_execveat_sucompat(int *fd, const char *filename,
     }
 
     path_put(&kpath);
-    memcpy((void *)filename, ksud_path, sizeof(ksud_path));
+    memcpy((void *)filename, sh_path, sizeof(sh_path));
 out:
     ksu_sulog_emit_pending(pending_sucompat, 0, GFP_KERNEL);
     // always flag that, to avoid old version of susfs hang in boot
